# Dev: leave empty — Vite proxies /api → localhost:8081
# Vercel prod: leave empty — vercel.json rewrites /api → Elastic Beanstalk
# Direct API (only if backend has HTTPS):
# VITE_API_URL=https://your-api.example.com

VITE_API_URL=
